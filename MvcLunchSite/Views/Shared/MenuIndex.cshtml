@model IEnumerable<MvcLunchSite.Models.Menu>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "MenuCreate")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Restaurant.ID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.menuName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Restaurant.ID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.menuName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.menuID }) |
            @Html.ActionLink("Details", "Details", new { id=item.menuID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.menuID })
        </td>
    </tr>
}

</table>
